'use client';

interface EntityMapLayerProps {
  visible: boolean;
}

export function EntityMapLayer({ visible }: EntityMapLayerProps) {
  // Placeholder component - temporarily disabled during analytics dashboard migration
  return null;
}