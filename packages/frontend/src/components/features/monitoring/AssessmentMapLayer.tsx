'use client';

interface AssessmentMapLayerProps {
  visible: boolean;
}

export function AssessmentMapLayer({ visible }: AssessmentMapLayerProps) {
  // Placeholder component - temporarily disabled during analytics dashboard migration
  return null;
}